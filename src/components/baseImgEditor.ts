import { BASE_EDITOR_CONFIG } from "../scripts/constants"
const baseImgEditor = {
    name:'baseImgEditor',
    component: 'BaseImg',
    config: {
        style: {
            ...BASE_EDITOR_CONFIG
        }
    }
}

export default baseImgEditor;